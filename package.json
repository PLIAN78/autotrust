{
  "name": "autotrust",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix api run dev\" \"npm --prefix apps/web run dev\"",
    "dev:web": "npm --prefix apps/web run dev",
    "dev:api": "npm --prefix api run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
